# proxyflix
Projeto em Java que implementa o padrão de projeto Proxy (GoF) em um sistema de streaming. O Proxy é utilizado para controlar o acesso a conteúdos premium, aplicar cache de filmes já carregados e realizar o carregamento sob demanda (lazy loading), simulando um cenário real de otimização e segurança em aplicações.
